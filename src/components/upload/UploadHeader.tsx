export function UploadHeader() {
    return (
      <section>
        <p className="mb-2 text-sm text-zinc-400">Upload</p>
        <h1 className="text-3xl font-semibold tracking-tight text-white">
          Upload a new lab report
        </h1>
        <p className="mt-2 max-w-2xl text-sm text-zinc-400">
          Add a PDF, image, or CSV file to extract biomarkers, review detected
          values, and save the report into your health history.
        </p>
      </section>
    );
  }